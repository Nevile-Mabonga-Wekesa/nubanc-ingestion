{
  "event_name": "raw.payload.received.v1",
  "domain": "raw",
  "description": "A raw payload has been received and recorded without interpretation.",
  "schema_version": 1,
  "immutability": "append-only",
  "required_metadata": {
    "event_id": "uuid",
    "tenant_id": "string",
    "occurred_at": "iso-8601-timestamp",
    "ingested_at": "iso-8601-timestamp",
    "source_identity": "string",
    "schema_version": "integer"
  },
  "payload": {
    "type": "object",
    "description": "Opaque payload. No assumptions are made at this stage."
  },
  "rules": [
    "This event represents a fact, not a command",
    "Payload is stored exactly as received",
    "No validation or transformation is allowed",
    "Schema may only evolve by version increment"
  ]
}
